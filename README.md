# PostgreSQL Practice

This repository contains my PostgreSQL practice work from Week 5.

## What I Practiced

- Setting up PostgreSQL
- Creating databases and tables
- Primary keys and constraints
- Inserting and retrieving data
- SQL queries and filtering
- Sorting and grouping data
- CRUD operations
- Foreign keys and table relationships
- INNER JOIN and LEFT JOIN
- SERIAL and sequences
- UUIDs

## Files

- `practice.sql` - Basic PostgreSQL practice queries
- `queries.sql` - SQL query exercises
- `crud_relationships.sql` - CRUD and relationship practice
- `MOCK_DATA.csv` - Sample student data

## Database

The main practice database used was:

`postgresql_practice`

The project includes a `students` table along with related `courses`, `enrollments`, and `uuid_students` tables.